# Self_driving_car_model
Deep Learning Self-Driving Car (Udacity Simulator + NVIDIA DNN) This repository implements a self-driving car model in the Udacity Simulator using NVIDIA's DNN architecture. Explore data augmentation, pre-processing, and model performance (details in Discussion). Instructions for setup and usage coming soon (Usage).
